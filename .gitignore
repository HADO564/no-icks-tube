# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Packaged builds
*.zip
*.xpi
web-ext-artifacts/
.amo-upload-uuid

# Node (if tooling is added later)
node_modules/

# Secrets — never commit real credentials
.env
.env.*
!.env.example
